[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bisync"
version = "0.1.0"
description = "Android app for bidirectional file sync via rclone"
requires-python = ">=3.12"
dependencies = [
]

[tool.setuptools]
py-modules = []
